import { apiFetcher } from '../js/model/apiFetcher.js';

test('검색어를 입력하면 동영상 list를 불러올 수 있다.', () => {
  const apiFetcher1 = new apiFetcher();
  //   return apiFetcher1.getList().then((data) => {
  //     console.log(data);
  //     expect(data).toBe();
  //   });
});
